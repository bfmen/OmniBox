{
  "name": "omnibox",
  "version": "1.0.0",
  "description": "OmniBox - Enhanced Web Proxy Worker for Cloudflare",
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "preview": "wrangler preview",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "npx eslint src/**/*.ts",
    "typecheck": "npx tsc",
    "typecheck:verbose": "npx tsc --verbose",
    "clean": "rm -rf node_modules package-lock.json",
    "install:clean": "npm run clean && npm install"
  },
  "keywords": [
    "cloudflare-workers",
    "proxy",
    "omnibox",
    "web-proxy",
    "kv-cache"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.1",
    "@eslint/js": "^10.0.1",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.2",
    "jiti": "^2.6.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "wrangler": "^4.60.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
